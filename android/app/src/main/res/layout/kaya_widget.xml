<?xml version="1.0" encoding="utf-8"?>
<!-- Kaya home-screen widget: obsidian card, hand-drawn feel, one lane bar. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/kaya_widget_bg"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="14dp"
    android:paddingBottom="14dp">

    <!-- headline row: brand mark · title · ping -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:src="@drawable/ic_widget_kaya"
            android:contentDescription="@string/app_name" />

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="@string/widget_title_standby"
            android:textColor="#F4F6F4"
            android:textSize="15sp"
            android:textStyle="bold"
            android:letterSpacing="0.02"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/widget_ping"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="@string/widget_ping_idle"
            android:textColor="#9BA3A0"
            android:textSize="12sp"
            android:fontFeatureSettings="tnum"
            android:maxLines="1" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="@string/widget_sub_off"
        android:textColor="#9BA3A0"
        android:textSize="12sp"
        android:maxLines="1"
        android:ellipsize="end" />

    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- route refresh: re-learn the fastest resolver path, fresh ping -->
    <TextView
        android:id="@+id/widget_refresh"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:gravity="center"
        android:text="@string/widget_refresh"
        android:textColor="#9BA3A0"
        android:textSize="11sp"
        android:letterSpacing="0.14"
        android:maxLines="1" />

    <!-- lane chip: the whole bar is the toggle -->
    <LinearLayout
        android:id="@+id/widget_chip"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/kaya_widget_chip_off"
        android:paddingStart="14dp"
        android:paddingEnd="14dp">

        <View
            android:id="@+id/widget_dot"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:background="@drawable/kaya_widget_dot_idle" />

        <TextView
            android:id="@+id/widget_chip_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:text="@string/widget_chip_arm"
            android:textColor="#F4F6F4"
            android:textSize="13sp"
            android:textStyle="bold"
            android:letterSpacing="0.12" />
    </LinearLayout>
</LinearLayout>
