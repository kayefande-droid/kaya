<?xml version="1.0" encoding="utf-8"?>
<!-- Plain black splash: safe to inflate on every API level. The Kaya logo
     appears the moment Flutter's first frame renders (milliseconds later). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/black" />
</layer-list>
