name: kaya
description: >-
  Kaya — a hand-built fast lane & launcher for mobile FPS gaming.
  Smart Anycast DNS steering, ping HUD, boost locks. Zero servers, 100% on-device.
publish_to: "none"
version: 1.1.9+15

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
